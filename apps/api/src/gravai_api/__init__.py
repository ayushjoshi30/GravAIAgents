"""GravAI REST API — the contract Graviton and the console call.

The MCP server is a thin adapter over these same services, so a capability can
never exist on one surface and be missing from the other.
"""

from __future__ import annotations

__version__ = "0.1.0"

__all__ = ["__version__"]
