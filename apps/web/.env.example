# Base URL of the GravAI REST API. The console reads this in the browser.
NEXT_PUBLIC_GRAVAI_API_BASE=http://localhost:8000

# Shown in the console footer so an operator knows which deployment they are on.
NEXT_PUBLIC_GRAVAI_ENV=local
